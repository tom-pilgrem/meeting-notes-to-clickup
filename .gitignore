__pycache__/
*.pyc
.venv/
venv/
.env
*.log
service-account.json

# state.json and run_log.jsonl are intentionally tracked, not ignored --
# the GitHub Actions workflow commits them back after every run since
# runners are ephemeral and have nothing else to persist state to. See
# CLAUDE.md "Cloud Scheduling" section.
